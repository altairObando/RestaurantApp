import React from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-paper';

export default function HomeLayout(){
    return <View>
        <Text> Render Home Layout Options </Text>
    </View>
}